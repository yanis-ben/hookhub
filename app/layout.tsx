import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "HookHub - Discover Claude Code Hooks",
  description: "Browse our curated collection of open-source Claude Code hooks to automate your development workflow. Find hooks for formatting, testing, git operations, and more.",
  openGraph: {
    title: "HookHub - Discover Claude Code Hooks",
    description: "Browse our curated collection of open-source Claude Code hooks to automate your development workflow.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
